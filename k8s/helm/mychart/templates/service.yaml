apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: app-namespace
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8000
  type: ClusterIP