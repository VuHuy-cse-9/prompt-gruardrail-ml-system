image:
  repository: docker.io/vnminhhuy2001/prompt-guardrail-service
  # This sets the pull policy for images.
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

ingress:
  host: 34.126.114.91.nip.io

namespace: "model-serving"