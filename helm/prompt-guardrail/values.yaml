image:
  repository: docker.io/vnminhhuy2001/prompt-guardrail-service
  # This sets the pull policy for images.
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1.66"

ingress:
  host: app.huy-fsds.info.pro.vn

namespace: "model-serving"